// Central asset management
// Import all images here for better organization and reusability

// Logo
import logo from './img/logo.png';

// Background images
import image39 from './img/Image 39.png';
import image40 from './img/Image 40.png';

// React logo (example)
import reactLogo from './react.svg';

// Export all assets
export const images = {
  logo,
  image39,
  image40,
  reactLogo,
};

// You can also organize by category
export const backgrounds = {
  leftDecor: image39,
  rightDecor: image40,
};

export const logos = {
  main: logo,
  react: reactLogo,
};

// Default export for convenience
export default images;
